{
  "id": "angular-language-server",
  "name": "Angular Language Server",
  "check": {
    "fileExists": {
      "name": "Check if Angular Language Server is installed",
      "file": "${serverHome}/node_modules/.bin/ngserver*"
    }
  },
  "run": {
    "exec": {
      "name": "Install Angular Language Server",
      "workingDir": "${serverHome}/node_modules",
      "command": {
        "windows": "npm.cmd install @angular/language-server @angular/language-service typescript@5 --force",
        "default": "npm install @angular/language-server @angular/language-service typescript@5 --force"
      },
      "onSuccess": {
        "configureServer": {
          "name": "Configure Angular Language Server command",
          "command": {
            "windows": "${serverHome}/node_modules/.bin/ngserver.cmd --stdio --tsProbeLocations ${serverHome}/node_modules/node_modules --ngProbeLocations ${serverHome}/node_modules/node_modules",
            "default": "${serverHome}/node_modules/.bin/ngserver --stdio --tsProbeLocations ${serverHome}/node_modules/node_modules --ngProbeLocations ${serverHome}/node_modules/node_modules"
          }
        }
      }
    }
  }
}
