{
  "id": "intelephense",
  "name": "Intelephense",
  "check": {
    "fileExists": {
      "name": "Check if Intelephense is installed",
      "file": "${serverDist}/node_modules/.bin/intelephense*"
    }
  },
  "run": {
    "exec": {
      "name": "Install Intelephense",
      "workingDir": "${serverDist}/node_modules",
      "command": {
        "windows": "npm.cmd install intelephense --force",
        "default": "npm install intelephense --force"
      },
      "onSuccess": {
        "configureServer": {
          "name": "Configure Intelephense command",
          "command": {
            "windows": "${serverDist}/node_modules/.bin/intelephense.cmd --stdio",
            "default": "${serverDist}/node_modules/.bin/intelephense --stdio"
          }
        }
      }
    }
  }
}
