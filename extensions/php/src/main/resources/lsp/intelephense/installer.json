{
  "id": "intelephense",
  "name": "Intelephense",
  "check": {
    "fileExists": {
      "name": "Check if Intelephense is installed",
      "file": "${serverHome}/node_modules/.bin/intelephense*"
    }
  },
  "run": {
    "exec": {
      "name": "Install Intelephense",
      "workingDir": "${serverHome}/node_modules",
      "command": {
        "windows": "npm.cmd install intelephense --force",
        "default": "npm install intelephense --force"
      },
      "onSuccess": {
        "configureServer": {
          "name": "Configure Intelephense command",
          "command": {
            "windows": "${serverHome}/node_modules/.bin/intelephense.cmd --stdio",
            "default": "${serverHome}/node_modules/.bin/intelephense --stdio"
          }
        }
      }
    }
  }
}
