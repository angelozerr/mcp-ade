{
  "id": "buildx-dockerfile-installer",
  "name": "Docker Buildx DAP",
  "check": {
    "exec": {
      "name": "Check if Docker Buildx DAP is available",
      "command": {
        "windows": "docker.exe buildx version",
        "default": "docker buildx version"
      },
      "timeout": 5000
    }
  },
  "run": {
    "configureServer": {
      "name": "Configure Docker Buildx DAP command",
      "command": {
        "windows": "docker.exe buildx dap build",
        "default": "docker buildx dap build"
      }
    }
  }
}
