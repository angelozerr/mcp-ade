{
  "check": {
    "fileExists": {
      "name": "Check if Gradle BSP server is installed",
      "file": "${serverHome}/extension/lib/server.jar"
    }
  },
  "run": {
    "download": {
      "name": "Download vscode-gradle",
      "openvsx": {
        "namespace": "vscjava",
        "extensionName": "vscode-gradle"
      },
      "output": {
        "dir": "${serverHome}"
      },
      "onSuccess": {
        "configureServer": {
          "name": "Configure Gradle BSP server command",
          "command": "java -Dplugin.dir=\"${serverHome}/extension/server/plugins\" -cp \"${serverHome}/extension/lib/*\" com.microsoft.java.bs.core.Launcher",
          "update": true
        }
      }
    }
  }
}
