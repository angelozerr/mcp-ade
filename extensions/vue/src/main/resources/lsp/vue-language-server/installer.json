{
  "id": "vue-language-server-installer",
  "name": "vue-language-server",
  "check": {
    "fileExists": {
      "name": "Check if vue-language-server is installed",
      "file": "${serverDist}/node_modules/.bin/vue-language-server*"
    }
  },
  "run": {
    "exec": {
      "name": "Install @vue/language-server",
      "workingDir": "${serverDist}/node_modules",
      "command": {
        "windows": "npm.cmd install @vue/language-server --force",
        "default": "npm install @vue/language-server --force"
      },
      "onSuccess": {
        "configureServer": {
          "name": "Configure vue-language-server command",
          "command": {
            "windows": "${serverDist}/node_modules/.bin/vue-language-server.cmd --stdio",
            "default": "${serverDist}/node_modules/.bin/vue-language-server --stdio"
          }
        }
      }
    }
  }
}
