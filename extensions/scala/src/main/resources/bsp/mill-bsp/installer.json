{
  "check": {
    "fileExists": {
      "file": "${workspaceFolder}/.bsp/mill-bsp.json"
    }
  },
  "run": {
    "exec": {
      "command": "mill mill.bsp.BSP/install",
      "workingDir": "${workspaceFolder}"
    }
  },
  "documentation": "Mill BSP requires the Mill BSP connection file (.bsp/mill-bsp.json). Runs 'mill mill.bsp.BSP/install' to generate it."
}
