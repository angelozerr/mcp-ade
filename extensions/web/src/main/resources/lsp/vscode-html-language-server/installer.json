{
  "id": "vscode-html-language-server-installer",
  "name": "vscode-html-language-server",
  "check": {
    "fileExists": {
      "name": "Check if vscode-html-language-server is installed",
      "file": "${serverHome}/node_modules/.bin/vscode-html-language-server*"
    }
  },
  "run": {
    "exec": {
      "name": "Install vscode-langservers-extracted",
      "workingDir": "${serverHome}/node_modules",
      "command": {
        "windows": "npm.cmd install vscode-langservers-extracted --force",
        "default": "npm install vscode-langservers-extracted --force"
      },
      "onSuccess": {
        "configureServer": {
          "name": "Configure HTML language server command",
          "command": {
            "windows": "${serverHome}/node_modules/.bin/vscode-html-language-server.cmd --stdio",
            "default": "${serverHome}/node_modules/.bin/vscode-html-language-server --stdio"
          }
        }
      }
    }
  }
}
