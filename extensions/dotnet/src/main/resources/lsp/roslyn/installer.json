{
  "id": "roslyn-installer",
  "name": "Roslyn Language Server",
  "runtime": "dotnet",
  "check": {
    "fileExists": {
      "name": "Check if Roslyn Language Server is installed",
      "file": {
        "windows": "${serverHome}/roslyn-language-server.cmd",
        "default": "${serverHome}/roslyn-language-server"
      }
    }
  },
  "run": {
    "exec": {
      "name": "Install Roslyn Language Server",
      "command": "dotnet tool install roslyn-language-server --tool-path ${serverHome} --prerelease",
      "onSuccess": {
        "configureServer": {
          "name": "Configure Roslyn Language Server command",
          "command": {
            "windows": "${serverHome}/roslyn-language-server.cmd --stdio",
            "default": "${serverHome}/roslyn-language-server --stdio"
          }
        }
      }
    }
  }
}
