{
  "id": "roslyn-installer",
  "name": "Roslyn Language Server",
  "runtime": "dotnet",
  "check": {
    "fileExists": {
      "name": "Check if Roslyn Language Server is installed",
      "file": {
        "windows": "${serverDist}/roslyn-language-server.cmd",
        "default": "${serverDist}/roslyn-language-server"
      }
    }
  },
  "run": {
    "exec": {
      "name": "Install Roslyn Language Server",
      "command": "dotnet tool install roslyn-language-server --tool-path ${serverDist} --prerelease",
      "onSuccess": {
        "configureServer": {
          "name": "Configure Roslyn Language Server command",
          "command": {
            "windows": "${serverDist}/roslyn-language-server.cmd --stdio",
            "default": "${serverDist}/roslyn-language-server --stdio"
          }
        }
      }
    }
  }
}
