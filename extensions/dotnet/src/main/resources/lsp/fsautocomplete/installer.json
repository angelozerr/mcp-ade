{
  "id": "fsautocomplete-installer",
  "name": "fsautocomplete",
  "runtime": "dotnet",
  "check": {
    "fileExists": {
      "name": "Check if fsautocomplete is installed",
      "file": {
        "windows": "${serverDist}/fsautocomplete.cmd",
        "default": "${serverDist}/fsautocomplete"
      }
    }
  },
  "run": {
    "exec": {
      "name": "Install FsAutoComplete",
      "command": "dotnet tool install fsautocomplete --tool-path ${serverDist}",
      "onSuccess": {
        "configureServer": {
          "name": "Configure fsautocomplete command",
          "command": {
            "windows": "${serverDist}/fsautocomplete.cmd --adaptive-lsp-server-enabled",
            "default": "${serverDist}/fsautocomplete --adaptive-lsp-server-enabled"
          }
        }
      }
    }
  }
}
