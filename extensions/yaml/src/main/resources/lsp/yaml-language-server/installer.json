{
  "id": "yaml-language-server",
  "name": "YAML Language Server",
  "check": {
    "fileExists": {
      "name": "Check if yaml-language-server is installed",
      "file": "${serverDist}/node_modules/.bin/yaml-language-server*"
    }
  },
  "run": {
    "exec": {
      "name": "Install yaml-language-server",
      "workingDir": "${serverDist}/node_modules",
      "command": {
        "windows": "npm.cmd install yaml-language-server --force",
        "default": "npm install yaml-language-server --force"
      },
      "onSuccess": {
        "configureServer": {
          "name": "Configure yaml-language-server command",
          "command": {
            "windows": "${serverDist}/node_modules/.bin/yaml-language-server.cmd --stdio",
            "default": "${serverDist}/node_modules/.bin/yaml-language-server --stdio"
          }
        }
      }
    }
  }
}
