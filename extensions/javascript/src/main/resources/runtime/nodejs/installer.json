{
  "id": "nodejs",
  "name": "Node.js",
  "description": "JavaScript runtime required by Node.js-based language servers",
  "url": "https://nodejs.org",
  "check": {
    "exec": {
      "name": "Check if Node.js is available",
      "command": "node --version",
      "timeout": 5000
    }
  },
  "run": {
    "download": {
      "name": "Download Node.js",
      "nodejs": {
        "release-type": "lts"
      },
      "output": {
        "dir": "${serverHome}",
        "stripRootDir": true
      },
      "onSuccess": {
        "configureServer": {
          "name": "Configure Node.js",
          "command": {
            "windows": "${serverHome}/node.exe",
            "default": "${serverHome}/bin/node"
          }
        }
      }
    }
  }
}
